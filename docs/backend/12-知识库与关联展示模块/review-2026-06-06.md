# 模块 12 — 知识库与关联展示 Review (2026-06-06)

## 检查范围

| 文件 | 状态 |
|------|------|
| `app/models/knowledge.py` | ✅ |
| `app/api/knowledge.py` | ✅ |
| `app/services/knowledge_service.py` | ✅ 修复 1 处懒加载 |
| `docs/backend/12-知识库与关联展示模块/模块设计.md` | ✅ 完善 |
| `docs/backend/12-知识库与关联展示模块/API接口.md` | ✅ 规范 |
| `docs/backend/12-知识库与关联展示模块/测试.md` | ✅ 详细 |
| `tests/test_knowledge_api.py` | ✅ 8 用例 |
| `tests/test_knowledge_service.py` | ✅ 16 用例 |

## 端点覆盖

| 端点 | 测试 |
|------|------|
| `GET /api/knowledge/nodes` | 4（列表/过滤/搜索/auth） |
| `GET /api/knowledge/nodes/{id}` | 2（详情/404） |
| `POST /api/knowledge/rebuild` | 2（重建成功/角色限制） |
| `GET /api/posters/{id}/related` | 1（在 test_posters_api.py） |
| **合计** | **9** |

## 发现的问题

### 1. 懒加载（已修复）

`_node_specs_for_poster()` 中有 2 处 `from .dict_manager import normalize`。提到模块顶部为 `_dict_normalize`，保留 try/except 降级逻辑。

### 2. 覆盖率缺口

- `POST /rebuild` 的 `source_type` 过滤路径未测
- `POST /rebuild` 的 embeddings 重建路径未测（需 Celery + pgvector）
- 失败回滚路径未测

建议后续补一个 `source_type` 过滤的 rebuild 测试。

## 文档质量

模块 12 的文档是三件套中最完善的：
- 模块设计.md 包含完整的 5 步生成机制、4 条关系规则、两级超链接模型
- API接口.md 每个端点都有字段说明表和处理流程
- 测试.md 有关键词映射表、关系规则表、字典标准化说明

## 覆盖率

| 文件 | 覆盖率 |
|------|--------|
| `api/knowledge.py` | 88% |
| `services/knowledge_service.py` | 94% |
| `models/knowledge.py` | 97% |
## 第二轮增强 (2026-06-06)

新增 corner-case 测试，覆盖边界条件、异常路径、多用户隔离。
