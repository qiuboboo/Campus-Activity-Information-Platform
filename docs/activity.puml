@startuml 校园活动信息平台-海报发布活动图
start
:用户登录系统;
:录入官方活动文本或导入抓取内容;
if (AI 服务可用?) then (是)
  :OpenClaw 辅助提取标题、时间、地点、主办方、主题;
else (否)
  :使用规则、正则和字典提取结构化字段;
endif
:生成活动海报草稿;
if (是否需要修改?) then (是)
  :用户修改海报内容;
endif
:系统保存海报;
:提交审核;
:管理员审核海报内容;
if (审核通过?) then (是)
  :匹配知识节点;
  :按时间、地点、主办方、主题建立海报关系;
  :发布活动海报;
else (否)
  :驳回并返回修改意见;
  if (是否需要修改?) then (是)
    :用户修改海报内容;
    :重新提交审核;
  endif
endif
stop
@enduml
