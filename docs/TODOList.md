# TODO List: Next Task

> **使用规范**：
> 1. 每完成一项，把 `[ ]` 改成 `[x]`
> 2. 在 `[x]` 后补充一句结果，例如：`- [x] 完成数据库备份脚本 - 结果：已生成并验证恢复`
> 3. 全部完成后，把本文件归档到 `docs/todos/`，再创建下一轮 TODO

## 默认约定

- 服务器操作用户：`workspace`
- 项目目录：`/home/workspace/Campus-Activity-Information-Platform`
- 运行方式：Docker Compose（v1.29）
- 已就绪：认证、海报 CRUD、审核发布、知识图谱、搜索、数据源、爬虫、Celery 异步队列、活动治理、审计日志、导出接口、API 文档、冒烟测试、备份恢复脚本
- 本轮暂无具体计划，待远程仓库更新 TODO 后再执行。

```bash
# 当前状态
cd /home/workspace/Campus-Activity-Information-Platform
git status --short --branch
git pull --ff-only
```
