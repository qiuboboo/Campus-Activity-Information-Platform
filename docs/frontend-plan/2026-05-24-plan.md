# 今日前端计划 — 2026-05-24

## 优先级说明

- **P0**: 阻塞性任务，影响核心体验
- **P1**: 重要功能，按依赖顺序推进
- **P2**: 完善性任务，可并行或延后

---

## P0

- [ ] **[P0] 实现搜索页面** — 3h — 依赖 SEARCH_DESIGN.md；包括搜索框、结果列表、筛选器、分页
- [ ] **[P0] 搭建前端测试框架并编写首个用例** — 2h — 引入 Vitest + @vue/test-utils，配置测试环境，为 AuthStore 编写单元测试

## P1

- [ ] **[P1] 修复登录后 `?redirect=` 参数未生效** — 0.5h — 依赖 P0 搜索页或详情页就绪后方可验证完整链路
- [ ] **[P1] 验证新的 mock 服务器能覆盖所有已实现页面** — 1h — 确认 `mock/` 下的端点覆盖首页、登录、注册所需数据
- [ ] **[P1] 实现活动详情页骨架** — 2h — 至少包含基本信息展示 + 报名入口（"功能建设中"占位可接受）

## P2

- [ ] **[P2] 清理已删除文件的引用** — 0.5h — 检查是否有文件仍引用旧的 `mock-server.js` 路径
- [ ] **[P2] 启用 TypeScript strict 模式** — 1h — 开启 `strict: true`，修复现有类型错误
- [ ] **[P2] 首页搜索框支持 `Enter` 提交（即便跳转搜索页）** — 0.5h — 当前仅弹出提示，应至少保留路由跳转能力
- [ ] **[P2] 为 `docs/frontend-design/` 中各文档补充实现后的偏差记录** — 1h — 将实现中的调整回写到设计文档
