# 从零开始后端与服务器选型指南

本文把这次项目的启动顺序拆成一条可执行路线：先买对服务器，再把后端跑起来，最后部署上线。

## 1. 先决定买哪种服务器

这一步先不要纠结“大厂里哪家最强”，先按项目阶段选。

### 适合你当前这个课程项目的方案

- 如果你的用户主要在中国内地，而且后面愿意备案：优先买中国内地轻量应用服务器。
- 如果你只是想先快速演示，不想先做备案：优先买中国香港轻量应用服务器。
- 如果你后面准备扩成多人协作、持续运行、并发更高：再升级到标准云服务器 ECS / CVM。

## 2. 我给你的直接建议

以下价格与备案判断，是我按 `2026-05-06` 查询腾讯云官方页面和官方文档后整理的，购买当天仍要以控制台实时价格为准。

### 推荐方案 A：先买腾讯云轻量应用服务器

原因很简单：

- 套餐和价格公开得比较清楚。
- 很适合课程项目和第一版后端。
- 后续要切换到更完整的云服务器也容易。

### 当前可参考的官方套餐信号

腾讯云官方轻量应用服务器页面当前能直接看到的公开套餐里，至少包括：

- 中国内地 `2核 2GB 40GB`：`45元/月`，`459元/年`
- 中国内地 `4核 8GB 120GB`：`230元/月`，`2346元/年`

这说明：

- 轻量应用服务器确实适合你这种课程项目和小型 Web 后端。
- 如果你购买当天页面没有正好出现 `2核 4GB`，可以优先看同系列相近套餐，或者直接切到标准云服务器。

### 建议配置

#### 方案 A1：国内正式版

- 地域：上海、广州、南京任选一个离目标用户近的
- 系统：`Ubuntu 22.04 LTS`
- 配置：`2核 4GB`
- 磁盘：`60GB` 起步

适合：

- Flask 后端
- PostgreSQL
- Redis
- 小规模课程演示

#### 方案 A2：免备案演示版

- 地域：中国香港
- 系统：`Ubuntu 22.04 LTS`
- 配置：`2核 4GB`

适合：

- 先做演示
- 先联调前后端
- 先跑接口和数据库

代价：

- 对中国内地访问延迟更高
- 成本通常高于国内低配活动机

## 3. 买服务器前必须知道的事

### 中国内地服务器

如果你后面要绑定域名并对外正式访问，通常要做 ICP 备案。腾讯云官方备案文档明确说明：使用中国大陆境内服务器开办网站或 APP，需要先完成 ICP 备案后才能开通访问。

### 中国香港 / 海外服务器

优点是不用先做内地备案，部署更快。

缺点是：

- 中国内地访问延迟通常更高
- 网络稳定性和访问体验不如内地机房

## 4. 你现在该买什么

如果你现在的目标是“先把后端搭起来并能演示”，直接按下面买：

### 最稳妥选择

- 云厂商：腾讯云轻量应用服务器
- 地域：如果不想备案就选中国香港；如果愿意备案就选上海
- 镜像：`Ubuntu 22.04 LTS`
- 配置：优先 `2核 4GB`；如果当天没有合适套餐，最低先选 `2核 2GB`，更稳妥选 `4核 8GB`
- 磁盘：`60GB` 及以上
- 时长：先买 `1个月` 或 `3个月`

## 5. 服务器买完后的初始化动作

买完服务器后，第一时间做这些：

1. 重置实例密码或配置 SSH 密钥。
2. 安全组放行端口：`22`、`80`、`443`、`5000`。
3. 登录服务器，更新系统：

```bash
sudo apt update && sudo apt upgrade -y
```

4. 安装基础工具：

```bash
sudo apt install -y git curl unzip
```

## 6. 推荐部署方式

第一版推荐用 Docker Compose，原因是最省心：

- Flask
- PostgreSQL
- Redis

都可以一起起。

服务器上执行：

```bash
sudo apt install -y docker.io docker-compose-plugin
sudo systemctl enable docker
sudo systemctl start docker
```

然后把当前项目上传到服务器，在 `backend` 目录执行：

```bash
cp .env.example .env
docker compose up -d --build
```

## 7. 这次已经帮你落下来的后端基础

项目里已经新增了一个 `backend` 目录，包含：

- Flask 后端骨架
- 登录鉴权
- 海报基础接口
- Dockerfile
- `docker-compose.yml`
- Nginx 反向代理示例

所以你的下一步不是再写文档，而是：

1. 先确定买国内还是香港服务器。
2. 把这个后端先在本地跑起来。
3. 再把同一套东西部署到服务器。

## 8. 我建议的执行顺序

1. 本地启动 `backend`
2. 用默认管理员登录拿到 token
3. 测试海报接口
4. 购买服务器
5. 把项目传到服务器
6. 用 Docker Compose 启动
7. 接 Nginx
8. 最后再接前端
