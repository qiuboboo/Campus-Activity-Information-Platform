# 中山大学活动管理系统 - 前端功能需求文档

> **版本**: 1.0  
> **前端开发者**: Chenrui Zhang  
> **最后更新**: 2026-05-24

> **实现状态更新（2026-07-13）**：当前已实现并注册公开搜索、活动列表、个人中心、个人日历、发布者活动管理/编辑、管理员看板/审核/数据源/审计日志等路由。页面的实现计划、接口/Mock 边界和验收标准以 [全站建设工作清单](frontend-plan/2026-07-13-full-site-build-checklist.md) 为准；未列在路由表中的功能不会暴露入口。

---

## 目录

1. 技术栈与约定
2. 页面树与路由表
3. 组件树
4. API 接口清单（Mock 格式）
5. 权限对应表
6. 关键交互流程说明
7. Mock 数据说明
8. 开发规划

---

## 1. 技术栈与约定

| 类别      | 选型 / 位置                              | 备注                                                                 |
| --------- | ---------------------------------------- | -------------------------------------------------------------------- |
| 核心框架  | Vue 3 (Composition API, `<script setup>`) | 使用 `<script setup>`，项目使用 TypeScript（`.ts` / `.vue` + TS）      |
| 构建工具  | Vite                                     | 开发服务器端口为 `3000`（见 `vite.config.ts` 中 `server.port`）      |
| UI 组件库 | Element Plus                             | 使用 `element-plus` + `@element-plus/icons-vue`                       |
| HTTP 请求 | Axios                                    | 统一使用 `src/api/client.ts` 中创建的 Axios 实例，`baseURL` 使用 `/api` |
| 路由      | Vue Router 4                             | 路由表定义在 `src/router/index.ts`                                    |
| 状态管理  | Pinia                                    | 全局状态（用户/权限）由 Pinia 管理                                    |
| 语言/类型 | TypeScript                               | 项目为 TS 项目，启用了 `vue-tsc` 类型检查                             |
| 代码风格  | 驼峰命名，组件名 PascalCase               | 组件文件与组合式函数使用清晰命名与注释                                 |

**全局约定**：

- 开发时前端通过 Vite 代理将 `/api` 前缀转发到本地后端（`http://127.0.0.1:5000`），因此 Axios 实例的 `baseURL` 在代码中设为 `'/api'`（见 `src/api/client.ts`）。
- 前端语义名：页面/组件中使用 `Activity`（活动）作为业务术语，API 路径与 Mock 端点一致使用 `/activities`。
- 错误处理由 `src/api/client.ts` 的响应拦截器统一：网络错误提示、HTTP 401/403 特殊处理（登出或跳转登录）、500+ 时提示服务器错误。
- 权限与鉴权：登录后将 `token` 存入 `localStorage`（`token`、`user`），路由守卫在 `src/router/index.ts` 的 `beforeEach` 中检查登录状态并处理权限跳转。
- Mock 服务位于 `frontend/mock/`；本地可启动 `node mock/index.js`（默认监听 5000），或使用 Vite 代理直接连接后端服务。

---

## 2. 页面与路由设计

本项目路由（位于 `src/router/index.ts`）当前仅注册以下页面：

| 路径 | 页面组件 | 说明 |
| ---- | -------- | ---- |
| `/` | `views/home/HomeView.vue` | 首页（已实现） |
| `/auth/login` | `views/auth/login/LoginView.vue` | 登录页（已实现） |
| `/auth/register` | `views/auth/register/RegisterView.vue` | 注册页（已实现） |
| `/activity/:id` | `views/activity/ActivityDetail.vue` | 活动详情页（已实现） |
| `/:pathMatch(.*)*` | `views/error/NotFound.vue` | 404 页面 |

**后续规划（具体路径待考虑）**：
- `/search` → 搜索结果页（待实现）
- `/profile` → 用户个人中心（待实现）
- `/admin` → 管理后台（待实现）
- `/calendar` → 活动日历页（待实现）
- `/about` → 关于页（待实现）
- `/contact` → 联系我们页（待实现）
- 注：上述路径与页面组件需在后续开发中逐步添加到路由表中，并实现对应的 Vue 组件。

---

## 3. 项目目录结构

```
frontend/
├── src/
│   ├── api/
│   │   ├── client.ts          # Axios 实例（baseURL='/api'）
│   │   ├── auth.ts            # 认证接口（登录/注册/验证码/用户信息）
│   │   ├── calendar.ts        # 日历事件接口
│   │   └── activities.ts      # 活动列表接口（get /activities）
│   ├── components/
│   │   ├── NavHeader.vue       # 顶部导航栏
│   │   ├── NavSidebar.vue      # 左侧导航菜单
│   │   ├── HomeCalendarCard.vue# 日历日程卡片
│   │   ├── FooterBar.vue       # 页脚
│   │   ├── SearchBar.vue       # 搜索框（首页顶部）
│   │   ├── AuthLayout.vue      # 认证页布局容器
│   │   ├── AuthHeader.vue      # 认证页标题区域
│   │   ├── AuthBackground.vue  # 认证页背景装饰
│   │   └── activity/
│   │       ├── ActivityHeader.vue # 活动详情头部信息
│   │       ├── ActivityBody.vue   # 活动正文与附件
│   │       └── ActivityMeta.vue   # 标签与统计信息
│   ├── composables/
│   │   └── useHomePage.ts      # 首页逻辑（轮播、日历、分类筛选、数据加载）
│   ├── stores/
│   │   └── auth.ts             # Pinia 认证状态（token、user、角色判断）
│   ├── router/
│   │   └── index.ts            # Vue Router 路由表与守卫
│   ├── utils/
│   │   └── authRules.ts        # 表单验证规则
│   ├── views/
│   │   ├── home/
│   │   │   └── HomeView.vue    # 首页（三栏布局、轮播、列表、日历）
│   │   ├── activity/
│   │   │   └── ActivityDetail.vue # 活动详情页（标题、正文、统计、操作区）
│   │   ├── auth/
│   │   │   ├── login/
│   │   │   │   └── LoginView.vue   # 登录页
│   │   │   └── register/
│   │   │       └── RegisterView.vue# 注册页（含邮箱验证码）
│   │   └── error/
│   │       └── NotFound.vue    # 404 页面
│   ├── App.vue                 # 根组件
│   ├── main.ts                 # 入口文件
│   └── style.css               # 全局样式
├── mock/
│   ├── index.js                # Mock 服务入口（端口 5000）
│   ├── db.js                   # 内存数据存储
│   ├── utils.js                # Mock 工具函数（解析 body、token 提取）
│   └── routes/
│       ├── health.js           # GET /api/health
│       ├── auth.js             # 认证相关（4 个端点）
│       ├── activity.js         # GET /api/activities、GET /api/activities/:id
│       └── calendar.js         # GET /api/calendar/events
├── index.html
├── package.json
├── vite.config.ts              # Vite 配置（代理 /api → 127.0.0.1:5000）
└── tsconfig.json
```

---

## 4. API 接口清单（Mock 格式）

> 以下列出 `frontend/mock/` 服务实际注册的端点。启动 Mock 后监听 `http://localhost:5000`，前端通过 Vite 代理 `/api` 前缀转发至此。
>
> 前端 `src/api/activities.ts` 请求路径为 `/activities`，与 Mock 端点 `/api/activities` 对齐。

### 4.1 健康检查

```
GET /api/health
```

无需鉴权。返回示例：

```json
{
  "status": "ok",
  "service": "campus-activity-frontend-mock",
  "timestamp": "2026-05-24T12:00:00.000Z"
}
```

### 4.2 认证模块

#### 登录

```
POST /api/auth/login
Content-Type: application/json
```

请求体：

```json
{
  "username": "admin",
  "password": "admin123456"
}
```

成功响应（200）：

```json
{
  "token": "mock-jwt-token-1716547200000",
  "user": {
    "id": 1,
    "username": "admin",
    "role": "admin",
    "created_at": "2026-01-01T00:00:00"
  }
}
```

失败响应（401）：

```json
{ "message": "invalid credentials" }
```

**预置测试账号**（见 `mock/db.js`）：

| 用户名 | 密码 | 角色 |
|--------|------|------|
| `admin` | `admin123456` | admin（管理员） |
| `zhangsan` | （需查看 db.js 或无密码默认匹配） | publisher（发布者） |

#### 获取当前用户

```
GET /api/auth/me
Authorization: Bearer <token>
```

若 `Authorization` 头携带有效 token，返回 `DEMO_USER`（管理员）：

```json
{ "user": { "id": 1, "username": "admin", "role": "admin", "created_at": "2026-01-01T00:00:00" } }
```

若无 token 或无效：

```json
{ "user": null }
```

#### 发送邮箱验证码

```
POST /api/auth/send-code
Content-Type: application/json
```

请求体：

```json
{ "email": "user@example.com" }
```

成功：

```json
{ "message": "验证码已发送", "code": "123456" }
```

（Mock 模式下验证码直接打印在控制台并返回在响应体中。）

#### 注册（含邮箱验证）

```
POST /api/auth/register
Content-Type: application/json
```

请求体：

```json
{
  "username": "newuser",
  "password": "pass123",
  "email": "user@example.com",
  "verification_code": "123456"
}
```

成功：

```json
{
  "token": "mock-jwt-token-1716547200000",
  "user": {
    "id": 3,
    "username": "newuser",
    "role": "viewer",
    "email": "user@example.com",
    "created_at": "2026-05-24T12:00:00.000Z"
  }
}
```

校验规则（Mock）：
- `username` 长度 2–50
- `password` 最短 6 位
- `email` 需含 `@`
- `verification_code` 必须与发送的验证码一致

### 4.3 活动列表

```
GET /api/activities?page=1&per_page=10&status=published&keyword=xx
```

无需鉴权。查询参数：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `page` | int | 否 | 页码，默认 1 |
| `per_page` | int | 否 | 每页条数，默认 10 |
| `status` | string | 否 | 筛选：`published`、`draft` 等 |
| `keyword` / `q` | string | 否 | 模糊搜索标题/摘要/正文/地点/组织者 |

成功响应：

```json
{
  "items": [
    {
      "id": 1,
      "title": "中山大学第12届学术科技节",
      "raw_text": "一年一度的学术科技节即将开幕...",
      "summary": "涵盖学术讲座、科技展览、创新竞赛的综合性学术活动。",
      "event_time": "2026-06-15T09:00:00",
      "location": "广州校区南校园",
      "organizer": "中山大学团委",
      "status": "published",
      "activity_type": "讲座",
      "created_at": "2026-05-20T10:00:00"
    }
  ],
  "total": 8,
  "page": 1,
  "per_page": 10,
  "pages": 1
}
```

### 4.4 日历事件

```
GET /api/calendar/events
Authorization: Bearer <token>
```

需鉴权。返回该用户的当日日程：

```json
{
  "events": [
    { "time": "09:00", "title": "审核待发布活动", "type": "info" },
    { "time": "14:00", "title": "服务器维护", "type": "warning" },
    { "time": "16:30", "title": "团队周会", "type": "info" }
  ]
}
```

若未提供有效 token，返回空数组：

```json
{ "events": [] }
```

### 4.5 前端调用对照

| 前端文件 | 实际请求路径 | Mock 端点 | 状态 |
|----------|-------------|-----------|------|
| `src/api/auth.ts` | `/auth/login` → `/api/auth/login` | `POST /api/auth/login` | ✅ 匹配 |
| `src/api/auth.ts` | `/auth/me` → `/api/auth/me` | `GET /api/auth/me` | ✅ 匹配 |
| `src/api/auth.ts` | `/auth/register` → `/api/auth/register` | `POST /api/auth/register` | ✅ 匹配 |
| `src/api/auth.ts` | `/auth/send-code` → `/api/auth/send-code` | `POST /api/auth/send-code` | ✅ 匹配 |
| `src/api/activities.ts` | `/activities` → `/api/activities` | `GET /api/activities` | ✅ 匹配 |
| `src/api/activities.ts` | `/activities/:id` → `/api/activities/:id` | `GET /api/activities/:id` | ✅ 匹配 |
| `src/api/calendar.ts` | `/calendar/events` → `/api/calendar/events` | `GET /api/calendar/events` | ✅ 匹配 |

---

## 5. 权限对应表

角色定义来源于 Mock 数据及 Pinia auth store（`src/stores/auth.ts`）。

| 角色 | 描述 | 前端判断 | 可见操作 |
|------|------|----------|----------|
| `admin` | 管理员 | `isAdmin`（`user.role === 'admin'`） | 审核活动、系统管理、查看所有内容；登录后侧边栏显示管理入口 |
| `publisher` | 发布者 | `isPublisher`（`user.role === 'publisher'`） | 创建/管理活动；可以访问发布相关功能 |
| `viewer` | 普通用户 | 已登录但 role 非 admin/publisher | 浏览、评论、收藏、查看个人中心（待实现） |
| guest | 未登录 | `!isLoggedIn` | 仅浏览首页与公开内容；点击需要登录的功能时跳转登录页 |

**当前实现情况**：
- 路由守卫区分已登录/未登录（未登录访问受限路由跳转 `/auth/login`）。
- 组件内可使用 `auth.isAdmin` / `auth.isPublisher` 控制按钮/区域显示。
- 更细粒度的权限判断（如“仅创建者可编辑”）待后续实现。

---

## 6. 关键交互流程说明

### 6.1 登录流程

1. 用户在 `LoginView` 输入用户名+密码，点击提交。
2. `src/stores/auth.ts` 中的 `login()` 调用 `POST /api/auth/login`。
3. Mock 验证用户名密码（预置：`admin/admin123456`），返回 token 与用户信息。
4. Store 将 `token` 和 `user` 写入 `localStorage`，并设置 Axios 请求头 `Authorization: Bearer <token>`。
5. 路由守卫检测到已登录，重定向至首页 `/`。

### 6.2 注册流程

1. 用户在 `RegisterView` 输入邮箱，点击“发送验证码”。
2. 调用 `POST /api/auth/send-code`，Mock 控制台打印验证码并返回在响应中。
3. 用户输入验证码、用户名、密码，提交注册。
4. 调用 `POST /api/auth/register`，校验通过后返回 token 和 user（role 为 `viewer`）。
5. 自动登录并跳转首页。

### 6.3 首页加载流程

1. 进入 `/`，`HomeView` 挂载后 `useHomePage` 的 `fetchData()` 执行。
2. 并发调用两次 `listActivities()`：
   - 一次筛选 `status=published` → 热点活动（`hotActivities`，用于轮播）。
   - 一次获取全部活动 → 最新活动（`recentActivities`，用于分类列表）。
3. 若已登录，同时调用 `getCalendarEvents()` 获取当日日程。
4. 热点轮播自动 4 秒切换；分类 Tab 切换时前端过滤 `activity_type`。

### 6.4 退出登录

1. 点击 NavSidebar 或 AppHeader 中的退出按钮。
2. `auth.logout()` 清除 `localStorage` 中的 `token` 和 `user`，重置 Pinia 状态。
3. 路由跳转至首页 `/`，页面变为未登录状态。

### 6.5 查看活动详情

1. 在活动列表或轮播中点击某活动卡片。
2. 路由跳转至 `/activity/:id`，组件加载时调用 `GET /api/activities/:id` 获取详情数据。
3. 展示活动完整信息。
4. 报名、分享、收藏按钮当前统一提示“内容未实现！”，用于明确标识未接入的交互。

---

## 7. Mock 数据说明

### 数据源

Mock 所有数据存放于 `frontend/mock/db.js`，服务重启后重置为初始状态（注册用户会丢失）。

### 预置活动（ACTIVITIES）

共 9 条，覆盖多种分类与状态：

| id | 标题 | 分类 | 状态 |
|----|------|------|------|
| 1 | 中山大学第12届学术科技节 | 讲座 | published |
| 2 | 校园十大歌手决赛之夜 | 晚会 | published |
| 3 | ACM程序设计校内选拔赛 | 竞赛 | published |
| 4 | 粤港澳大湾区青年论坛 | 论坛 | published |
| 5 | 中山大学春季艺术展 | 展览 | published |
| 6 | 腾讯2026校园招聘宣讲会 | 招聘 | published |
| 7 | 夏季校园马拉松 | 体育 | published |
| 8 | 摄影技巧分享工作坊 | 其他 | published |
| 9 | 秋季社团招新嘉年华 | 其他 | draft |

### 预置用户

| 用户名 | 密码 | 角色 | 说明 |
|--------|------|------|------|
| `admin` | `admin123456` | admin | 管理员，可看到全部日程事件 |
| `zhangsan` | `123456` | publisher | 发布者，在 `REGULAR_USER` 中定义 |

注册新用户默认 role 为 `viewer`。

### 日历事件

按用户 ID 索引：
- 用户 1（admin）：3 个事件（审核、维护、周会）
- 用户 2（zhangsan）：4 个事件（筹备会、检查草稿、场地确认、彩排）

---

## 8. 开发规划

### 当前已完成

- 项目脚手架：Vue 3 + TypeScript + Vite + Pinia + Vue Router + Element Plus
- 页面：首页（三栏布局、轮播、分类、日历）、登录、注册、404
- 页面：活动详情页（详情头部、正文、附件、统计与操作区）
- 组件：NavHeader、NavSidebar、FooterBar、HomeCalendarCard、SearchBar、AuthLayout/AuthHeader/AuthBackground、ActivityHeader、ActivityBody、ActivityMeta
- API 层：Axios 客户端、认证 API、活动列表 API、活动详情 API、日历 API
- Mock 服务：完整的 Mock API 服务器（认证、活动列表、活动详情、日历、健康检查）
- 测试：前端单元测试基线已添加，活动详情页具备首个 Vitest 用例
- 文档：本需求文档、frontend-plan 开发任务清单

### 短期（待实现 — 可参考 SEARCH_DESIGN.md）

- 搜索页面（SearchPage） — 关键字搜索、结果列表、筛选、分页
- 创建/编辑活动页（ActivityEditor） — 表单校验、富文本/图片上传

### 中期

- 用户个人中心（MyProfile）
- 管理后台：审核面板（AuditList）、用户管理
- 集成真实后端认证与 API，替换 Mock
- 前端单元测试框架（Vitest + @vue/test-utils）

### 长期

- E2E 测试
- 移动端适配、无障碍审计
- 性能优化（图片懒加载、虚拟列表等）

> 详细任务清单请参见 `frontend/docs/frontend-plan/2026-05-24-plan.md` 与 `frontend/docs/frontend-plan/2026-05-24-status.md`。
