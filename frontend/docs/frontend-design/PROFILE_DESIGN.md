# 个人中心设计

## 目标与入口

- 路径：`/profile`，仅登录用户可用；顶部用户入口进入。
- 用户查看账户、角色、收藏、订阅、通知和近期报名，能修改显示名并退出登录。

## 布局与状态

- 顶部沿用公开站导航，内容使用资料卡和标签页；窄屏下卡片纵向排列。
- 标签页为概览、收藏、订阅、通知；各页均提供加载、空数据、失败和重试状态。

## 接口与验收

- 使用 `GET/PATCH /auth/me`、收藏/订阅/通知接口；Mock 提供 viewer、publisher、admin 数据。
- 验收：访客重定向登录；保存资料后刷新一致；未读数在标记已读后同步更新。
