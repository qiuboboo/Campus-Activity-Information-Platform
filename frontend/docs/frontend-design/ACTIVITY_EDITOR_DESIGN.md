# 活动创建与编辑设计

## 路径与权限

- 路径：`/activities/create`、`/activities/:id/edit`；仅 publisher/admin 可访问。
- 编辑页以服务端返回的可编辑状态为准，前端不绕过权限。

## 表单与行为

- 字段：标题、摘要、正文、时间、地点、主办方、类型、标签、附件。
- 支持保存草稿、提交审核、离开未保存提醒；服务端字段错误定位到表单项。
- 附件显示上传进度、失败与删除确认；Mock 用 URL 模拟已上传附件。

## 验收

- 新建保存后进入我的活动；编辑刷新后保留已保存数据；草稿可提交审核。
