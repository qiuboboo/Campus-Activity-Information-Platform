# 我的日历设计

## 目标与入口

- 路径：`/calendar`，仅登录用户可用；首页日历卡片和个人中心均可进入。
- 显示已报名活动与个人日程，支持切换月份、按日查看和进入活动详情。

## 布局与状态

- 主区为月视图，右侧/下方为选中日事件列表；事件按“活动”“个人日程”区分颜色。
- 小屏将事件列表置于月历下方；空日和无日程显示明确提示。

## 接口与验收

- `GET /calendar/events?month=YYYY-MM` 返回事件及可选 `activity_id`。
- 验收：月份和选中日变化正确更新事件；带活动 ID 的项目能打开详情。
