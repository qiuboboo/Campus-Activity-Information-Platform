# 今日前端任务清单 — 2026-05-24

说明：今日焦点为“活动详情页（Activity Detail）”的设计与可交付任务拆解。文档包含：设计目标、组件拆分、API 合同、关键交互流程、验收标准与估时。

## 今日目标

- 完成活动详情页的设计规范（组件树、接口合同、交互流程、Mock 示例）。
- 在完成设计后，实施骨架组件并补齐 Mock 端点，保证本地可联调。

## 优先级说明

- **P0**: 今日必须完成的任务（活动详情页设计与 Mock 合同）。
- **P1**: 实现与联调相关的重要任务（组件实现、路由、测试）。
- **P2**: 清理、优化与文档完善。

---

## P0（今日核心 — 活动详情页设计）

- [x] 设计并实现活动详情页（ActivityDetail）骨架 — 4h — 负责人：@owner
	- 验收标准：通过 `/activity/:id` 能展示以下字段并正确排版：
	  - 标题、摘要、正文（raw_text）、时间（event_time）、地点、组织者、活动类型、状态（published/draft）
	  - 附件/图片列表、标签、统计（views、registrations）
	  - 行为控件：报名按钮、分享、收藏、举报（受权限限制）
	- 可交付文件：`views/activity/ActivityDetail.vue`、`src/components/activity/ActivityHeader.vue`、`ActivityBody.vue`、`ActivityMeta.vue`。

- [x] 确认并补齐 Mock API：GET /api/activities/:id — 0.5h — 负责人：@owner
	- 验收标准：Mock 返回与前端字段一致的示例 JSON（含 attachments、meta 等）；能够在本地通过 axios 拿到并渲染。

- [x] 编写界面验收用例（手工与一个基础单元测试） — 1h — 负责人：@owner
	- 验收标准：Vitest 单元测试断言 ActivityDetail 在给定 Mock 数据下渲染标题与时间。

---

## P1（重要实现与联调）


- [x] 实现详情页路由与重定向逻辑 `/activity/:id` — 0.5h — 负责人：@owner
	- 验收标准：支持 `?redirect=` 与 `?source=`，并保证 query 参数传递到目标页面。

---

## P2（改进与收尾）

- [x] 在文档中添加示例凭据与本地测试步骤 — 0.5h — 负责人：@owner
	- 示例凭据：
		- 管理员：用户名 `admin` / 邮箱 `admin@example.com`，密码 `admin123456`
		- 普通用户：用户名 `zhangsan` / 邮箱 `zhangsan@example.com`，密码 `123456`
	- 本地测试步骤（复制到终端运行）：
		```powershell
		# 在仓库根目录启动 mock（端口 5000）
		cd frontend
		node mock/index.js

		# 在另一个终端启动 Vite 开发服务器（端口 3000）
		npm install
		npm run dev

		# 运行单元测试（Vitest）
		npm run test:unit
		```

- [x] 清理旧引用、补全 Mock 覆盖率、更新设计差异记录 — 0.5h — 负责人：@owner
	- 目标：移除或替换代码/文档中残留的 `Poster` 相关命名，确保 Mock 覆盖 `GET /api/activities/:id`、列表、搜索场景。

- [x] 根据活动详情页的设计，撰写设计文档（`docs/frontend-design/ACTIVITY_DETAIL_DESIGN.md`） — 1h — 负责人：@owner
	- 验收标准：设计文档包含页面目标、布局结构、交互行为、数据来源、Mock 要求、响应式设计与可访问性说明。

---

## API 合同（快速参考）

- GET /api/activities/:id
	- 200 返回示例：
	{
	  "id": 1,
	  "title": "示例活动",
	  "summary": "简短摘要",
	  "raw_text": "活动正文 HTML 或 Markdown",
	  "event_time": "2026-06-15T09:00:00",
	  "location": "广州校区",
	  "organizer": "学生会",
	  "status": "published",
	  "activity_type": "讲座",
	  "attachments": [{"url":"/media/1.png","name":"海报"}],
	  "tags": ["学术","公开"],
	  "meta": {"views":123, "registrations":45},
	  "created_at": "2026-05-20T10:00:00"
	}

---

## 快速执行步骤（可直接转 Issue）

1. 设计：绘制详情页主区域与侧栏组件树（15-30min）。
2. Mock：补齐 GET /api/activities/:id 返回示例（15min）。
3. 实现：ActivityDetail.vue 骨架 + 样式（2.5h）。
4. 测试：写 1 个 vitest 用例验证核心渲染（1h）。
5. 联调：与后端/产品确认字段与交互（30min）。

如果确认，我可以把第 2 步与第 3 步拆成具体 PR 并开始实现。

